import { ScrollTrackerDirective } from './scroll-tracker.directive';
describe('ScrollTrackerDirective', function () {
    it('should create an instance', function () {
        var directive = new ScrollTrackerDirective();
        expect(directive).toBeTruthy();
    });
});
//# sourceMappingURL=scroll-tracker.directive.spec.js.map